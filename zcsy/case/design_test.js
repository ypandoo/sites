'use strict';

describe('myApp.design module', function() {

  beforeEach(module('myApp.design'));

  describe('design controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var designCtrl = $controller('designCtrl');
      expect(designCtrl).toBeDefined();
    }));

  });
});